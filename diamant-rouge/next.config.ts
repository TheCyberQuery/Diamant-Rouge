// next.config.js

/** @type {import('next').NextConfig} */
const nextConfig = {
    i18n: {
        locales: ['en', 'fr', 'ar'],
        defaultLocale: 'en',
    },
    // other config if needed...
}

module.exports = nextConfig;